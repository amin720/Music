
@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<!-- Bnr Head -->
<section class="bnr-head">
    <div class="container">
        <div class="position-center-center">
            <h3>دانلود آهنگ </h3>
            <p>
                پروژه موزیک
                الناز شبستری
                استاد دادگر
            </p>
        </div>
    </div>
</section>

<!-- Discover -->
<section class="discover discover-style-2">
    <div class="container">
        <div class="main-heading">
            <h3>جسجتو سبک موسیقی خود</h3>
            <hr>
        </div>

        <!-- Center Circle -->
        <div class="outer">
            <div class="center-part">
                <div class="position-center-center"> با سلیقه خود وارد شوید</div>
            </div>

            <!-- Bramds -->
            <div class="discover-music">
                <ul class="row">
                    @foreach (var item in ViewBag.ListGenres)
                    {
                        <li class="col-sm-4">
                            <a href="@Url.Action("BandWithGenre",new {genreId = item.Id})">@item.Name</a>
                            <img class="img-responsive" src="~/Library/images/dis-img-7.jpg" alt="@item.Name">
                        </li>
                    }

                </ul>
            </div>
        </div>
    </div>
</section>

<!-- Latest Release -->
<section class="latest-release">
    <div class="container">
        <div class="main-heading">
            <h3>آخرین منتشر شده ها</h3>
            <hr>
        </div>

        <!-- Latest Release -->

        <div class="row">
            @foreach (var item in ViewBag.LastReleaseSpecial)
            {
                <div class="col-md-6">
                    <article>
                        <img src="~/Library/images/relase-img-1.jpg" alt="">
                        <div class="botm-text">
                            <span>MUSIC VIDEO</span> <a href="#.">@item.FileName</a>
                        </div>
                    </article>
                </div>

                break;
            }


            <!-- Latest Release -->
            <div class="col-md-6">
                <div class="row">
                    @foreach (var item in ViewBag.LastRelease)
                    {
                        <div class="col-md-6">
                            <article>
                                <img src="~/Library/images/relase-img-2.jpg" alt="">
                                <div class="botm-text">
                                    <span>آهنگ</span> <a href="#.">@item.FileName</a>
                                </div>
                            </article>
                        </div>
                    }
                </div>
            </div>
        </div>
    </div>
</section>

